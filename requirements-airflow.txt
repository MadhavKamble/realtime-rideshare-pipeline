deltalake==0.17.4
pyarrow==15.0.2
mlflow==2.11.3
xgboost==2.0.3
# Must match requirements.txt — both containers train/evaluate models
scikit-learn==1.4.2
# Must match requirements.txt — both containers train/evaluate models
pandas==2.2.1
redis==5.0.1
python-dotenv==1.0.1