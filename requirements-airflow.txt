deltalake==0.17.4
pyarrow==15.0.2
mlflow==2.11.3
xgboost==2.0.3
# Pinned below requirements.txt's version: this container's base image
# (apache/airflow:2.8.1) bundles Python 3.8, and neither scikit-learn nor
# pandas ship Python-3.8-compatible wheels past these versions. Bumping
# these to match requirements.txt breaks `docker compose build airflow`.
scikit-learn==1.3.2
pandas==2.0.3
redis==5.0.1
python-dotenv==1.0.1
requests==2.31.0